{
  "name": "autorouter",
  "version": "0.1.0",
  "description": "Local request-routing gateway with inspectable model selection.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alanh-code/autorouter.git"
  },
  "bugs": {
    "url": "https://github.com/alanh-code/autorouter/issues"
  },
  "homepage": "https://github.com/alanh-code/autorouter#readme",
  "files": [
    "bin/gateway",
    "src",
    ".env.example",
    "LICENSE",
    "README.md"
  ],
  "bin": {
    "autorouter-gateway": "./bin/gateway"
  },
  "scripts": {
    "start": "node ./bin/gateway",
    "gateway": "node ./bin/gateway",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && node --check ./bin/gateway && node --check ./src/core/index.js && node --check ./src/core/model-id.js && node --check ./src/gateway/auth.js && node --check ./src/gateway/index.js && node --check ./src/gateway/network.js && node --check ./src/gateway/protocol.js && node --check ./src/gateway/server.js && node --check ./src/gateway/upstream-adapter.js && node --check ./src/gateway/upstream-credentials.js && npm test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.18"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
