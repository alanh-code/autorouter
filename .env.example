# Optional: copy to .env.local and fill in your OpenRouter key.
# Then run: npm run gateway -- configure-upstream openrouter
# After configuration, remove the key from .env.local; it is stored privately.
# The separate AutoRouter local key is generated automatically.
AUTOROUTER_UPSTREAM_API_KEY=
